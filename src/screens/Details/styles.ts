import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  scroll: {alignItems: 'center', gap: 20},
  image: {width: '100%', height: 300},
  nameText: {fontSize: 18, fontWeight: '600'},
});
