import {StyleSheet} from 'react-native';

const commonStyles = StyleSheet.create({
  list: {height: '100%'},
  addCartText: {backgroundColor: 'orange', padding: 5},
  icon: {width: 20, height: 20},
});

export const ITEM_HEIGHT = 170;

export default commonStyles;
